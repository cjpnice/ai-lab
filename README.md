# 电力数据智能分析与可视化团队网站

这是一个专业的实验室网站，采用纯 HTML + CSS + JavaScript 构建，可直接部署到 GitHub Pages。

## ✨ 特性

- 🎨 **独特的设计风格**: 工业科技风格，深色主题配合网格纹理和数据流动效果
- 📱 **完全响应式**: 支持桌面、平板和移动设备
- ⚡ **快速加载**: 无需构建工具，即开即用
- 📝 **Markdown 支持**: 使用 marked.js 渲染 Markdown 内容
- 🔄 **单页应用**: SPA 架构，流畅的页面切换
- 🎯 **自动加载**: 研究成果和新闻资讯自动扫描目录加载
- 🏷️ **元数据支持**: 支持 YAML frontmatter 定义文章属性

## 📁 项目结构

```
lab-website/
├── index.html          # 主页面
├── README.md           # 说明文档
├── package.json        # 项目配置和脚本
├── css/
│   └── style.css       # 样式文件
├── js/
│   └── app.js          # JavaScript 应用逻辑
└── content/            # Markdown 内容目录
    ├── about.md        # 团队介绍
    ├── contact.md      # 联系方式
    ├── research/       # 研究成果文章
    └── news/           # 新闻文章
```

---

## � 内容更新指南

本网站的内容分为多个模块，每个模块的更新方式如下：

### 1️⃣ 团队介绍

**更新位置**: `content/about.md`

直接编辑 Markdown 文件，使用标准 Markdown 语法编写团队介绍内容。

**示例**:
```markdown
# 团队介绍

我们团队专注于电力系统智能分析与可视化技术研究...
```

### 2️⃣ 联系方式

**更新位置**: `content/contact.md`

直接编辑 Markdown 文件，使用标准 Markdown 语法编写联系信息。

**示例**:
```markdown
# 联系我们

## 地址
某某大学某某楼

## 邮箱
contact@lab.edu.cn
```

### 3️⃣ 研究成果

1. **创建文章文件**
   - 在 `content/research/` 目录下创建新的 Markdown 文件
   - 建议使用英文名称，如 `new-research.md`

2. **添加元数据**
   - 在文件顶部添加 YAML frontmatter 或直接在正文中编写内容
   - 示例：
   ```markdown
   ---
   title: 项目标题
   description: 项目简短描述
   date: 2024-02
   icon: ⚡
   category: 预测算法
   ---

   # 项目标题

   项目详细介绍...
   ```

3. **更新索引**
   - 打开 `js/app.js` 文件
   - 在 `contentIndex.research` 数组中添加新的项目信息：
   ```javascript
   research: [
       // 现有项目...
       {
           id: 'new-research',              // 唯一标识符
           file: 'research/new-research.md', // 文件路径
           title: '新项目标题',
           description: '项目简短描述',
           icon: '🚀',
           date: '2024-02',
           category: '新技术'
       }
   ]
   ```

### 4️⃣ 新闻资讯

1. **创建新闻文件**
   - 在 `content/news/` 目录下创建新的 Markdown 文件
   - 建议使用日期格式命名，如 `2024-03-01-new-event.md`

2. **添加元数据**
   ```markdown
   ---
   title: 新闻标题
   excerpt: 新闻摘要
   date: 2024-03-01
   icon: 📰
   category: 学术交流
   ---

   # 新闻标题

   新闻详细内容...
   ```

3. **更新索引**
   - 在 `js/app.js` 的 `contentIndex.news` 数组中添加新闻信息：
   ```javascript
   news: [
       // 现有新闻...
       {
           id: '2024-03-01-new-event',
           file: 'news/2024-03-01-new-event.md',
           title: '新闻标题',
           excerpt: '新闻摘要',
           date: '2024-03-01',
           icon: '📰',
           category: '学术交流'
       }
   ]
   ```

### 5️⃣ 团队成员

**更新位置**: `js/app.js` 中的 `contentIndex.team` 数组

团队成员信息需要在 `js/app.js` 中直接配置：

```javascript
team: [
    {
        id: 'team-new',
        name: '姓名',
        title: '职位',
        avatar: '👨‍🔬',  // 使用 emoji 或图片 URL
        bio: '个人简介',
        email: 'email@lab.edu.cn',
        research: '研究方向'
    }
]
```

**注意**: 团队成员不使用 Markdown 文件，因为信息结构化程度高，直接配置更方便。

### 6️⃣ 学术出版物

**更新位置**: `js/app.js` 中的 `contentIndex.publications` 数组

出版物信息需要在 `js/app.js` 中直接配置：

```javascript
publications: [
    {
        id: 'pub-new',
        title: '论文标题',
        authors: '作者列表',
        venue: '期刊/会议名称',
        year: '2024',
        volume: '53(9)',
        pages: '9992-10008',
        doi: '10.1007/xxxxx',
        links: {
            pdf: '#',  // PDF 链接或 '#'
            doi: 'https://doi.org/...'  // DOI 链接
        }
    }
]
```

---

## 🚀 本地开发

### 安装依赖（可选）

```bash
npm install
```

### 启动本地服务器

```bash
npm start
# 或
npm run serve
```

访问 http://localhost:3000 查看网站。


---
