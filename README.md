# 电力数据智能分析与可视化团队网站

这是一个专业的实验室网站，采用纯 HTML + CSS + JavaScript 构建，可直接部署到 GitHub Pages。

## ✨ 特性

- 🎨 **独特的设计风格**: 工业科技风格，深色主题配合网格纹理和数据流动效果
- 📱 **完全响应式**: 支持桌面、平板和移动设备
- ⚡ **快速加载**: 无需构建工具，即开即用
- 📝 **Markdown 支持**: 使用 marked.js 渲染 Markdown 内容
- 🔄 **单页应用**: SPA 架构，流畅的页面切换
- 🎯 **自动加载**: 研究成果和新闻资讯自动扫描目录加载
- 🏷️ **元数据支持**: 支持 YAML frontmatter 定义文章属性

## 📁 项目结构

```
lab-website/
├── index.html          # 主页面
├── README.md           # 说明文档
├── CONTENT_GUIDE.md    # 内容管理详细指南
├── .gitignore          # Git 忽略文件
├── css/
│   └── style.css       # 样式文件
├── js/
│   └── app.js          # JavaScript 应用逻辑
└── content/            # Markdown 内容目录
    ├── about.md        # 团队介绍
    ├── contact.md      # 联系方式
    ├── research/       # 研究成果文章
    │   ├── power-load-forecasting.md
    │   └── data-visualization-platform.md
    └── news/           # 新闻文章
        ├── 2024-02-15-ieee-publication.md
        └── 2024-01-20-nsfc-grant.md
```

## 🛠️ 开发环境设置（可选）

如果需要使用 npm 脚本自动生成索引：

```bash
# 安装依赖（可选，用于使用 npm 命令）
npm install
```

## 🚀 部署到 GitHub Pages

### 方法一：通过 GitHub 网页界面部署

1. **创建 GitHub 仓库**
   - 登录 GitHub
   - 创建新仓库，命名为 `your-username.github.io` 或其他名称
   - 仓库可以设为公开或私有

2. **上传文件**
   - 将所有文件上传到仓库
   - 或者直接在网页上创建文件

3. **启用 GitHub Pages**
   - 进入仓库的 **Settings**（设置）
   - 点击左侧的 **Pages**
   - 在 **Source** 下选择：
     - Branch: `main`（或 `master`）
     - Folder: `/ (root)`
   - 点击 **Save**

4. **访问网站**
   - 等待几分钟部署完成
   - 访问 `https://your-username.github.io/your-repo-name/`

### 方法二：通过 Git 命令行部署

```bash
# 1. 克隆你的仓库（如果还没有）
git clone https://github.com/your-username/your-repo-name.git
cd your-repo-name

# 2. 复制所有网站文件到仓库目录
# （将 index.html, css/, js/, content/ 等复制进来）

# 3. 提交更改
git add .
git commit -m "Add lab website"
git push origin main

# 4. 在 GitHub 设置中启用 Pages（如上所述）
```

## 📝 内容管理

### ⚡ 自动加载功能（推荐）

研究成果和新闻资讯支持自动扫描目录加载：

1. **创建 Markdown 文件**
   - 研究成果放在 `content/research/` 目录
   - 新闻资讯放在 `content/news/` 目录
   - 在文件顶部添加 YAML frontmatter 元数据

2. **文件格式示例**

```markdown
---
title: 项目标题
description: 项目简短描述
date: 2024-01
icon: ⚡
category: 预测算法
---

# 项目标题

项目详细介绍...
```

### 手动配置（传统方式）

如果需要手动配置，编辑 `js/app.js` 中的 `contentIndex` 变量。

## 🎨 自定义样式

所有样式都在 `css/style.css` 中，使用 CSS 自定义属性方便修改：

```css
:root {
    /* 主色调 */
    --color-primary: #0a3d62;
    --color-accent: #00d2ff;

    /* 字体 */
    --font-display: 'Rajdhani', 'Noto Sans SC', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;

    /* 间距 */
    --spacing-md: 1rem;
    --spacing-lg: 1.5rem;
}
```

## 🔧 技术栈

- **HTML5**: 页面结构
- **CSS3**: 样式和动画
- **JavaScript (ES6+)**: 应用逻辑
- **marked.js**: Markdown 渲染（通过 CDN 加载）

## 📱 浏览器支持

- Chrome/Edge (推荐)
- Firefox
- Safari
- 移动浏览器

## 🐛 常见问题

### Q: Markdown 内容不显示？
A: 确保 `content/` 目录下的文件路径正确，检查浏览器控制台是否有错误。

### Q: 页面点击后不跳转？
A: 确保使用的是 Hash 路由（URL 中有 # 符号），这是 GitHub Pages 兼容的方式。

### Q: 在 GitHub Pages 上样式不正常？
A: 确保所有文件路径正确，CSS 和 JS 文件在正确的目录中。

## 📄 许可证

MIT License - 自由使用和修改

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题，请通过以下方式联系：
- 邮箱: contact@powerlab.edu.cn
- GitHub Issues: 在仓库中提交 Issue

---

**祝您使用愉快！** 🎉
